VITE_API_BASE=https://v2.api.noroff.dev
# Automatically create a Noroff API Key after successful login if one is not stored
VITE_AUTO_CREATE_API_KEY=true
# Hardcode an existing API key for local development (leave empty to auto-create)
VITE_API_KEY=
# Student email domain enforced during registration validation
VITE_STUDENT_EMAIL_DOMAIN=@stud.noroff.no
# Optional storage namespace prefix (used for localStorage keys)
VITE_STORAGE_PREFIX=auction